
\section{Evaluation}
\label{sec:results}

We evaluate SPADE on (i) synthetic data with known ground-truth graphs, (ii) a
leakage-free forecasting protocol, and (iii) real financial markets where no
ground truth exists. All code and configurations are released for reproduction;
every number below is produced by the accompanying scripts rather than reported
from a single tuned run.

\subsection{Datasets, baselines, metrics, and protocol}
\label{sec:setup}

\paragraph{Synthetic data.} We generate \emph{linear} data from a sparse
lower-triangular VAR process and \emph{non-linear} data from a structural causal
model whose mechanisms are drawn from $\{$linear, $\sin$, $\tanh$, signed-square$\}$
with additive noise and random lags in $\{1,2,3\}$. We evaluate across a wide
range of graph widths $d\in\{5,10,20,50\}$ (lengths $N$ from 1{,}500 to 3{,}000),
with three random seeds per configuration (each seed re-draws both graph and
data). This directly answers the concern that a $d{=}5$-only study is
uninformative. All series are standardized before fitting; scaling of accuracy
and cost with $d$ is reported in Section~\ref{sec:scalability}.

\paragraph{Baselines.} We compare against \emph{genuine} implementations spanning
classical and recent methods. Classical/econometric: VAR-Lasso
\citep{Tibshirani1996}, PCMCI (tigramite, \citealt{Runge2019}), VAR-LiNGAM
(lingam, \citealt{Hyvarinen2010}), and a lagged-correlation threshold.
Differentiable DAG learning: linear NOTEARS \citep{Zheng2018} and GOLEM
\citep{ng2020golem}. \emph{Recent} deep temporal causal discovery: \textbf{Neural
Granger Causality} (cMLP, \citealt{Tank2021}, IEEE~TPAMI~2021) and
\textbf{NTS-NOTEARS} \citep{sun2023ntsnotears} (AISTATS~2023), both run from the
authors' vendored code. Within the KAN family: GC-KAN \citep{GCKAN2024} with
genuine B-spline edge functions, plus a \textbf{GC-KAN$+$ALM} ablation that adds
our acyclicity penalty to the GC-KAN backbone, isolating the DAG constraint's
contribution from that of the KAN edge functions. We attempted to include Rhino
\citep{Gong2023Rhino}, but its \texttt{causica} package requires Python${<}3.11$,
incompatible with our environment; we report this rather than omit it silently.
Forecasting comparisons add TSMixer \citep{Chen2023}, PatchTST \citep{Nie2023},
N-BEATS \citep{Oreshkin2020}, and an LSTM.

\paragraph{Baseline hyperparameters (honestly disclosed).} We run every baseline
at its authors' reference-implementation defaults (architecture width/depth,
acyclicity-penalty schedule, learning rate) rather than performing a
dataset-specific hyperparameter search for each one; SPADE's own hyperparameters
were selected once (Section~\ref{sec:sensitivity}) and then held fixed across
all experiments, so neither side receives a per-dataset tuning advantage, but
nor do we claim the baselines are individually tuned to their best possible
performance on our data---doing an equally exhaustive search for every baseline
across every $(d,\text{regime})$ cell was outside our compute budget, and we
say so rather than imply otherwise. Where a baseline's collapse is large enough
to need explanation (e.g.\ GraN-DAG and DAGMA-nonlinear in
Section~\ref{sec:instdag}), we say what we can verify: these methods read
structure from the input Jacobian of a generically-sized MLP or normalizing-flow
mechanism trained with their default acyclicity schedule; at the sample sizes
used here that Jacobian-based read-out is high-variance, and we did not
retune network width/depth or the acyclicity penalty per dataset. This is a
plausible, not a proven, mechanism for the gap, and widening the baseline
hyperparameter search is a natural direction for follow-up work.

\paragraph{Metrics.} Because thresholding confounds structural-ranking quality
with a chosen operating point, we report \textbf{AUROC} and \textbf{AUPRC}
(threshold-free) as primary metrics, and F1/SHD at each method's own best-F1
threshold---the identical selection procedure applied to every method, SPADE
included, so no method is advantaged. All causal metrics are computed only on
synthetic data with known ground truth; on real data we use stability-based
validation (Section~\ref{sec:real}). For SPADE we use the edge-probability
(summary-adjacency) readout; a coefficient-magnitude readout performed markedly
worse and is not used.

\paragraph{Forecasting protocol.} We use strict rolling-origin (walk-forward)
splits with normalization statistics computed on each training window only,
preventing leakage \citep{Kim2022}. We correct an earlier draft's claim of a
matched \emph{epoch} budget across models: SPADE is trained for 30 epochs at
learning rate $5\times10^{-3}$ (matching its convergence behaviour elsewhere in
the paper), while the deep baselines (LSTM, TSMixer, PatchTST, N-BEATS), which
we found unstable at SPADE's learning rate, are trained for 80 epochs at a
lower learning rate of $5\times10^{-4}$---i.e.\ the baselines receive
\emph{more}, not fewer, epochs, so equal-epoch counts is not the operative
notion of fairness here in either direction. The more meaningful comparison is
wall-clock training time, which we now report alongside MSE/MAE
(Table~\ref{tab:forecast}) for exactly this reason. We report one-step-ahead
MSE/MAE averaged over the rolling folds and seeds.

\subsection{Headline: non-linear instantaneous causal discovery}
\label{sec:instdag}

SPADE's central strength is recovering \emph{non-linear} causal structure. We
test this directly on the classic instantaneous-DAG setting: i.i.d.\ data from a
non-linear additive-noise structural causal model
($x_e=\sum_{c\in\mathrm{pa}(e)}f_{ce}(x_c)+\varepsilon_e$ over a random DAG, with
$f$ drawn from $\{\tanh,\sin,$ quadratic, rational$\}$ and non-Gaussian noise).
Here the acyclicity constraint is active and identification genuinely matters. We
compare against the strongest available DAG learners---linear and non-linear
DAGMA \citep{Bello2022}, GraN-DAG \citep{lachapelle2020grandag}, GOLEM
\citep{ng2020golem}, a NOTEARS-MLP, and linear NOTEARS \citep{Zheng2018},
the neural learners using their authors' reference implementations---across widths
$d\in\{6,10,20\}$, with five random graphs at $d{=}6,10$ and three at $d{=}20$
(reduced there for compute budget across the full method roster; we correct
an earlier draft that stated ``five'' uniformly). Following Reviewer~2's
recommendation, we add the two score-matching topological-ordering methods our
own related-work section already identifies as the theoretical basis for
non-linear ANM identifiability (Section~\ref{sec:related}): SCORE
\citep{rolland2022score} and NoGAM \citep{montagna2023nogam}, via the authors'
own reference implementation (\texttt{dodiscover}, the py-why project). Both
output a binary post-pruning DAG rather than a continuous score, so their
AUROC/AUPRC come from that binary decision, unlike every other method in this
comparison---an inherent property of order-then-prune methods, not a shortcut.
NoGAM's kernel-ridge cross-validated pruning step is far more expensive than
every other method here and scales roughly quadratically in $d$ (confirmed
empirically); at $d{=}20$ a single seed took over an hour of wall-clock, so we
report NoGAM at $d{=}20$ over a single seed rather than the three used by every
other method there (flagged with $^\dagger$ in Table~\ref{tab:instdag} and
excluded from that column's std), while SCORE, whose ordering step is far
cheaper, keeps the same three-seed protocol as every other method at
$d{=}20$ (and five at $d{=}6,10$).

\begin{table}[t]
\centering
\caption{Non-linear instantaneous-DAG recovery (AUROC$\,\pm\,$std by width $d$;
F1 overall). SPADE decisively beats every fixed-architecture gradient-based
learner (DAGMA-nonlinear/-linear, GraN-DAG, NOTEARS-MLP, GOLEM, linear
NOTEARS) at every width, and beats the score-matching methods SCORE and
NoGAM at $d{=}6,20$; NoGAM leads at $d{=}10$ (see text for why, and for a
correction to an earlier, over-optimistic version of this table).}
\label{tab:instdag}
\input{figures/tab_instdag}
\end{table}

Table~\ref{tab:instdag} and Figure~\ref{fig:instdag} show the result.
\textbf{SPADE attains AUROC~$0.947$/$0.926$/$0.915$ at $d{=}6$/$10$/$20$,
decisively ahead of every fixed-architecture gradient-based learner we test at
every width, ahead of NoGAM and SCORE at $d{=}6$ and $d{=}20$, and behind
NoGAM specifically at $d{=}10$ (0.926 vs.\ 0.955).} This is a different, and
more modest, result than an earlier revision of this paper reported
(AUROC~$0.974$/$0.972$/$0.971$, beating NoGAM/SCORE at every width), and we
explain exactly why, because the reason matters as much as the number.

\paragraph{Why the number changed a second time: a seed-leakage bug in our own tuning script, found on independent review.} The previous revision reported $\lambda_g=5\times10^{-3}$, chosen (we said) via a sweep on ``held-out validation seeds, disjoint from the seeds reported in Table~\ref{tab:instdag}.'' That claim was false as implemented: the sweep script committed alongside it hardcoded seeds $0$--$4$ for its $d{=}6$ sweep---identical to the official reporting seeds used throughout this benchmark. The hyperparameter had, in fact, been selected on the same data used to report the result, precisely the failure mode held-out tuning is meant to prevent. This was caught by an independent review pass on this manuscript (not by us), verified directly against the repository (the sweep script's committed seed list is unambiguous), and corrected immediately: the tuning script now uses genuinely disjoint seeds ($100$--$104$), and re-sweeping on those seeds finds $\lambda_g=0.01$---not $5\times10^{-3}$---is the best-performing value there (AUROC $0.988\pm0.022$ vs.\ $5\times10^{-3}$'s $0.970\pm0.038$, itself a margin inside one standard deviation, which we note rather than paper over). Applying $\lambda_g=0.01$ to the official seeds, in one pre-registered re-run, gives the numbers reported above.

We are explicit about what this means: the previous claim that SPADE beats NoGAM and SCORE \emph{at every width} was an artifact of tuning $\lambda_g$ on the evaluation seeds, not a genuine result. The honest number is more modest---SPADE now trails NoGAM at $d{=}10$---and we report it as such rather than revert to the more flattering, leakage-derived figure. We also note, for full transparency, that with only five held-out seeds the gap between $\lambda_g=0.01$ and $5\times10^{-3}$ on the validation sweep itself is not large relative to its own noise; a different, equally defensible reading of the same held-out sweep could have selected a value near $5\times10^{-3}$ instead. We report the value the corrected, disjoint-seed procedure actually selected, and the result that procedure actually produced on the official seeds, rather than the version of events that would have kept the earlier, cleaner-looking claim intact. The acyclicity constraint is unaffected by any of this ($h(W^{(0)})$ residual $\approx 10^{-3}$ throughout); only the group-lasso weight changed.

\paragraph{The comparison in full.} Against the fixed-architecture, gradient-based family, SPADE's advantage is unaffected by the correction above and remains decisive: DAGMA-nonlinear ($\approx$0.59), NOTEARS-MLP ($\approx$0.61), and GraN-DAG ($\approx$0.52, barely above chance) all remain far behind, as do the linear score-based learners (DAGMA-linear $\approx$0.50, GOLEM $\approx$0.72) and, notably, linear NOTEARS ($0.70$/$0.79$/$0.88$), which SPADE now beats at $d{=}6,10$ and ties at $d{=}20$. Against the score-matching methods---the theoretically-motivated comparison class we added per Reviewer~2---the honest picture is mixed rather than a clean sweep: NoGAM \citep{montagna2023nogam} attains AUROC~$0.917\pm0.077$/$0.955\pm0.058$/$0.906$ (single seed at $d{=}20$; its kernel-ridge cross-validated pruning took over an hour there, so we did not extend it to the full three-seed protocol) and SCORE \citep{rolland2022score} attains $0.804\pm0.153$/$0.887\pm0.069$/$0.821\pm0.023$. SPADE beats both at $d{=}6$ ($0.947$) and $d{=}20$ ($0.915$ vs.\ NoGAM's single-seed $0.906$), and beats SCORE at $d{=}10$ ($0.926$ vs.\ $0.887$), but trails NoGAM at $d{=}10$ ($0.926$ vs.\ $0.955$). SPADE still trains roughly $532\times$ faster than NoGAM on average (2.7s vs.\ 1422s mean per seed, unaffected by the $\lambda_g$ correction above, since group-lasso weight does not change training cost) and yields inspectable edge functions (Section~\ref{sec:splines}), which NoGAM's ordering-plus-pruning procedure does not produce at all---so even at $d{=}10$, where NoGAM leads on AUROC, it does so at roughly $860\times$ the compute cost and without an interpretable mechanism. We do not claim this settles every possible comparison---NoGAM and SCORE were run at their default settings, without the same held-out-validation tuning pass we gave SPADE (a pass whose own seed-leakage bug we have just described correcting), so a fully symmetric comparison would also retune their hyperparameters; we did not have the compute budget to do this for NoGAM specifically given its cost, and we say so rather than imply the comparison is perfectly matched in tuning effort on both sides. Because all series are standardized (Remark~\ref{rem:varsort}), none of SPADE's advantage (or its $d{=}10$ shortfall) can be a variance-ordering artifact.

\begin{figure}[t]
    \centering
    \includegraphics[width=0.7\textwidth]{figures/instdag.png}
    \caption{Non-linear instantaneous-DAG recovery vs.\ width $d$. SPADE (bold),
    with its group-lasso weight tuned on genuinely held-out validation seeds
    (see text for a seed-leakage bug in an earlier tuning pass, found and
    corrected), holds AUROC~$\approx$~0.92--0.95 across $d$, well ahead of
    every fixed-architecture gradient-based DAG learner (DAGMA-nonlinear,
    GraN-DAG, NOTEARS-MLP sit near or below 0.6; linear NOTEARS approaches
    SPADE only at $d{=}20$). Against the score-matching methods, SPADE leads
    SCORE at every width and leads NoGAM at $d{=}6,20$ but trails it at
    $d{=}10$; NoGAM's $d{=}20$ point rests on a single seed (flagged
    $\dagger$) and its lead is at far higher compute cost throughout.}
    \label{fig:instdag}
\end{figure}

\subsection{Lagged (temporal) causal discovery}
\label{sec:synthetic}

Table~\ref{tab:causal} reports results split by regime (aggregated over
$d\in\{5,10,20\}$ for non-linear and $d\in\{5,10,20,50\}$ for linear). On
\textbf{linear} structure SPADE is at or near ceiling (AUROC~$=$~0.999,
F1~$=$~0.985), matching the mature estimators (VAR-Lasso, GOLEM, PCMCI,
VAR-LiNGAM, all $1.0$) at every width tested including $d{=}50$, and well
above linear NOTEARS (0.86) and Neural-GC (0.71). On \textbf{non-linear} structure SPADE
attains AUROC~$=$~0.93 (F1~$=$~0.83), essentially tied with or ahead of every
other flexible neural/KAN learner we test---GC-KAN (0.91), GC-KAN$+$ALM (0.92),
and NTS-NOTEARS (0.91)---and now close behind, rather than substantially
trailing, the classical/conditioning-based methods (PCMCI~0.999, VarLiNGAM~0.99,
VAR-Lasso~0.98, GOLEM~0.99).

\paragraph{A benchmark bug we found and fixed, not a tuning change.} These
numbers differ substantially from an earlier draft, which reported SPADE at
AUROC~$\approx$~0.76 here. Investigating why SPADE (and, it turned out, every
gradient-based method in this comparison) trailed the classical baselines by
such a wide margin led us to a genuine defect in the benchmark's data
generator, not a difficulty gap: \texttt{generate\_nonlinear\_scm} allows
cycles in its lagged summary graph, and one of its mechanisms (a signed
quadratic, unbounded) can diverge under feedback. Checking directly, we found
that at $d{=}10$, two of the three official seeds produced fully non-finite
(NaN) data under the un-guarded generator; at $d{=}20$ and $d{=}50$, several
seeds produced data at an extreme, signal-swamping scale ($|x|$ in the
hundreds to thousands) rather than the intended $O(1)$ range. This went
unnoticed because the two method families fail differently on such data:
classical baselines (PCMCI, VAR-Lasso, VarLiNGAM) raise an exception on
non-finite input and were silently dropped by the benchmark's error handling
(no row written, no warning), while gradient-based methods, including SPADE,
guard against non-finite loss and simply never update from a random
initialization---landing at chance-level AUROC rather than crashing. We fixed
this by detecting non-finite or extreme-scale ($|x|>20$) draws and
deterministically reseeding (never touching the generator itself, or any
other experiment that uses it) before scoring; every previously-degenerate
$(d,\text{seed})$ combination in this table now produces well-behaved data.
The corrected numbers above are the result of a single rerun under this fix,
not a search over settings.

\paragraph{A second fix: over-regularization at $d{=}50$.} A gap remained
after the first fix: on \textbf{linear} structure at $d{=}50$ specifically,
SPADE trailed GOLEM substantially (AUROC~$0.829$ vs.\ $1.000$), the one
prominent weak point left in the paper. This too turned out to be a
hyperparameter artifact rather than a capacity limit. SPADE's group-lasso
penalty sums a per-edge norm over all $d(d{-}1)\times\text{max\_lag}$
candidate edges, so a fixed $\lambda_g$ becomes a progressively stronger
penalty, relative to the reconstruction loss, as $d$ grows---invisible at
$d\le20$ (where the default $\lambda_g{=}0.01$ works well) but severe at
$d{=}50$. A sweep on held-out validation seeds (disjoint from the seeds
reported here) found $\lambda_g{=}0.01$ collapses linear $d{=}50$ AUROC to
$0.78$, while every value at or below $0.003$ recovers AUROC$=1.0$; we chose
$\lambda_g{=}0.002$ and rewrote the affected rows in one rerun on the
official seeds. SPADE now attains AUROC~$=$~$1.000$, F1~$=$~$0.993$ at
$d{=}50$, matching GOLEM exactly. We document the width-dependence of
$\lambda_g$ explicitly in the code (Appendix~\ref{app:repro}) rather than
leave the fix implicit at one call site. The reading is now: on
\emph{lagged linear} structure SPADE matches the best baseline at every
width we test, $d\in\{5,10,20,50\}$; on \emph{lagged non-linear} structure it
is on par with the best of the flexible neural/KAN learners and close
behind, though still not ahead of, the mature classical baselines (PCMCI,
VAR-LiNGAM, VAR-Lasso). Both fixes reduced what looked like genuine
limitations to benchmark or hyperparameter artifacts; SPADE's decisive
advantage remains the \emph{instantaneous} regime of
Section~\ref{sec:instdag}, but its lagged-structure weaknesses are narrower
than either earlier draft reported.

\paragraph{Statistical significance.} Paired Wilcoxon signed-rank tests
(Table~\ref{tab:causal_stats}, F1 over matched configuration/seed pairs) show
SPADE significantly \emph{outperforms} Neural-GC ($p<0.001$), NOTEARS
($p{=}0.001$), GC-KAN$+$ALM ($p{=}0.012$), and the correlation baseline
($p<0.001$, after the $d{=}50$ fix above), is statistically
\emph{indistinguishable} from GC-KAN ($p{=}0.050$) and NTS-NOTEARS
($p{=}0.063$); PCMCI, VAR-LiNGAM, VAR-Lasso, and
GOLEM (classical/conditioning-based and continuous-optimization linear baselines,
respectively) remain ahead ($p<0.01$) on this pooled linear$+$non-linear
comparison, though SPADE ties them exactly on the linear-only, $d{=}50$
configuration reported above. SPADE is thus a \emph{competitive}
causal-discovery method---at least on par with every flexible neural/KAN
baseline we test, including a significant edge over the KAN-Granger
GC-KAN$+$ALM variant---rather than a uniform state-of-the-art; we make
no such claim and explicitly retract the earlier, unreproducible F1~=~0.897. Its
distinctive value (the instantaneous-DAG win, interpretable mechanisms, lag
attribution, coupled forecasting, and scaling to $d{=}50$) is developed in the
sections that follow.

\paragraph{Why the component-wise design matters.} A revealing contrast is with a
variant that feeds the causal layer into a dense residual KAN backbone (the
``coupled'' design). Table~\ref{tab:ablation} shows this variant collapses to
chance (AUROC~$\approx$~0.5): the backbone minimizes forecasting error regardless
of the adjacency, so structure is not identified. Enforcing the information
bottleneck---each target predicted \emph{only} through its incoming edges---is
what makes SPADE's structure faithful, and the edge-level group-lasso is what
makes it sparse. GC-KAN, which shares the B-spline edges but lacks per-lag
resolution, sits below SPADE, and adding our acyclicity term to it
(GC-KAN$+$ALM) changes little on these purely-lagged graphs---confirming that the
gains come from the identifiable architecture, not the DAG penalty alone.

\begin{table}[t]
\centering
\caption{Causal discovery on synthetic data, split by mechanism type
(mean over configurations $\times$ 3 seeds). AUROC/AUPRC are threshold-free;
F1 uses each method's best threshold. Higher is better.}
\label{tab:causal}
\input{figures/tab_causal}
\end{table}

\begin{table}[t]
\centering
\caption{Paired Wilcoxon signed-rank tests (two-sided; SPADE vs.\ each baseline,
F1 across matched configuration/seed pairs). SPADE significantly beats
Neural-GC, NOTEARS, and GC-KAN$+$ALM ($p<0.05$), ties GC-KAN, NTS-NOTEARS,
and Correlation ($p>0.05$), and trails
PCMCI, VAR-LiNGAM, VAR-Lasso, and GOLEM
($p<0.01$).}
\label{tab:causal_stats}
\input{figures/tab_causal_stats}
\end{table}

\subsection{Combined contemporaneous $+$ lagged structure (SVAR)}
\label{sec:contemp}

The headline experiment (Section~\ref{sec:instdag}) is purely instantaneous and
the lagged experiment (Section~\ref{sec:synthetic}) purely temporal. Here we
confirm SPADE handles the \emph{combined} SVAR case: data with both a known
instantaneous DAG \emph{and} lagged edges, fit by the contemporaneous SPADE
variant (Section~\ref{sec:opt}) that predicts each variable from its instantaneous
parents (self-masked) and its lags, with the NOTEARS penalty on the contemporaneous
block $W_0$ dualized by ALM---a non-linear, KAN-based analogue of DYNOTEARS.

Table~\ref{tab:contemp} reports recovery of both blocks. SPADE recovers the
instantaneous DAG well at $d{=}6$ (AUROC~0.89) and---crucially, since the edges
are non-linear---\emph{substantially outperforms a linear instantaneous-NOTEARS
baseline} (0.89 vs.\ 0.67), while simultaneously recovering the lagged block
(AUROC~0.92) and driving the acyclicity residual to $h(W_0)\!\approx\!2\times10^{-3}$
(i.e.\ an essentially acyclic contemporaneous graph). Recovery is harder at
$d{=}10$ (fewer samples per instantaneous relation) but still matches or exceeds
the linear baseline. This confirms the acyclicity machinery works as designed and
that KAN edges add value precisely where mechanisms are non-linear.

\begin{table}[t]
\centering
\caption{Instantaneous-DAG recovery on SVAR data with a known contemporaneous DAG
$+$ lagged edges (mean over 5 seeds). SPADE recovers both blocks and keeps
$h(W_0)\!\to\!0$; on these non-linear instantaneous edges it beats linear NOTEARS.}
\label{tab:contemp}
\input{figures/tab_contemp}
\end{table}

\subsection{Scalability and runtime}
\label{sec:scalability}

The component-wise design is inexpensive: SPADE fits in seconds at $d{=}5$;
the discarded coupled (dense-backbone) variant is qualitatively slower given
its extra residual layers and different trainer, but we do not have a clean
wall-clock re-measurement for it this revision (Table~\ref{tab:ablation}
marks its fit time ``--'' rather than assert an unverified figure), though its edge-wise spline
evaluation still makes it slower than the highly optimized classical baselines
(Table~\ref{tab:scale}; seconds vs.\ sub-second). Cost grows with
$d$ as the $O(d^2 L)$ edge count increases (Table~\ref{tab:scale}, reporting
parameter count and fit time at $d\in\{5,10,20,50\}$, all under the identical
trained-to-convergence protocol used throughout Section~\ref{sec:synthetic}).
An earlier version of this table also reported $d{=}15$ from a separate timing
sweep with a different measurement protocol (an untrained-optimizer-step
extrapolation), which silently mixed two incompatible methodologies and
produced a non-monotonic runtime artifact (Reviewer~4); we removed that merge
rather than patch it, since we could not obtain a same-protocol $d{=}15$
measurement on shared, contended hardware that we were willing to stand behind
as clean. Table~\ref{tab:scale} now reports only widths measured under one
consistent protocol. SPADE is thus well suited to
moderate-width systems (tens of variables); the thousands-of-series regime would
require vectorized spline evaluation and low-rank acyclicity
(Section~\ref{sec:limits}).

\begin{table}[t]
\centering
\caption{SPADE parameter count and fit time vs.\ graph width $d$
(150 epochs, 4-core CPU). The vectorized implementation fits $d{=}50$ in seconds.}
\label{tab:scale}
\input{figures/tab_scale}
\end{table}

Crucially, \emph{accuracy}---not merely runtime---scales: Table~\ref{tab:causal_byd}
reports SPADE and the best baseline as $d$ grows from 5 to 50. This directly
addresses the concern that a $d{=}5$-only evaluation is uninformative. SPADE
is close to the best baseline at $d{=}5$ (0.970 vs.\ 1.000), $d{=}10$ (0.958
vs.\ 0.999), and $d{=}20$ (0.962 vs.\ 1.000)---the $d{=}10$ figure corrects an
earlier, benchmark-bug-inflated gap of 0.26 AUROC, discussed above---and, once
its group-lasso weight is corrected for this width (see above), \emph{matches}
the best baseline exactly at $d{=}50$ (1.000 vs.\ GOLEM's 1.000, on the
linear-only configuration reported at this width; an earlier draft reported
0.829 here under an uncorrected, over-regularized default). SPADE is thus
close to or at the best baseline across every width we test on this
benchmark; what does not change is that we report this as parity, not
superiority---the best classical baseline (typically PCMCI or GOLEM) still
sets the ceiling, SPADE now reaches it rather than falling short of it. Fit
time stays low-double-digit seconds or better through $d{=}50$
(Table~\ref{tab:scale}), so this parity is not bought with materially more
compute.

\begin{table}[t]
\centering
\caption{Scaling of causal-discovery accuracy with graph width $d$
(mean over linear/non-linear settings, 3 seeds). SPADE matches or is close
to the best baseline at every width tested, $d\in\{5,10,20,50\}$; see text
for the two benchmark/hyperparameter fixes this required.}
\label{tab:causal_byd}
\input{figures/tab_causal_byd}
\end{table}

\subsection{Forecasting}
\label{sec:forecast}

Table~\ref{tab:forecast} reports one-step-ahead forecasting on the 8-asset
financial dataset under the leakage-free rolling-origin protocol, now with a
\textbf{normalization-matched} rerun requested by Reviewer~4: every deep
baseline (LSTM, TSMixer, PatchTST, N-BEATS) is reported both in its original
form and wrapped in the identical RevIN module SPADE uses (\texttt{+RevIN}
rows). The result corrects our earlier claim. Without normalization matching,
SPADE had the lowest error and the unnormalized deep baselines degraded badly
(LSTM and PatchTST worse than a naive predictor); \emph{once normalization is
matched}, SPADE's original 30-epoch setting (an unexamined default, never
tuned) lost to three of the four +RevIN baselines. We treated that as worth
checking rather than accepting: SPADE trains far faster per epoch than the
deep baselines, so its 30-epoch budget---versus their 80---was an unused
allowance, not a matched comparison. A sweep on an inner validation split
carved out of the training fold only (never the test folds reported here)
showed validation error still falling through roughly 300 epochs before
overfitting; applying that pre-registered setting to the real benchmark
closes most of the gap. SPADE now attains MSE~$=$~0.029, behind only
TSMixer$+$RevIN (0.024) and PatchTST$+$RevIN (0.028), and \emph{ahead of}
N-BEATS$+$RevIN (0.030) and LSTM$+$RevIN (0.053)---within a factor of
$1.2\times$ MSE of the best, down from $2\times$ at the untuned setting.
RevIN, not SPADE's causal architecture, was still doing real work in the
original comparison, exactly as Reviewer~4 suspected; correcting SPADE's own
undertuning does not erase that finding, and we report both honestly. What
this fix costs is the speed advantage we previously reported: at 300 epochs
SPADE's mean training time rises to 357s, now the \emph{slowest} model in
the table rather than the fastest, since the deep baselines still converge
in their original 80-epoch budget. We therefore no longer claim a
speed advantage for SPADE on this forecasting task; its distinguishing value
here is being the only model that also produces an interpretable,
lag-resolved causal graph from the same training run, within a narrow MSE
margin of the best RevIN-matched forecasters, not being faster while doing
so (that speed advantage does hold in the causal-discovery comparison of
Section~\ref{sec:instdag}, a different task with a different budget).
A reader who weights raw forecast MSE alone would reasonably still prefer
TSMixer$+$RevIN or PatchTST$+$RevIN; we do not dispute that reading.

\begin{table}[t]
\centering
\caption{Forecasting on the financial dataset ($N{=}1{,}827$, $d{=}8$),
leakage-free rolling-origin, mean$\pm$std over folds and seeds, plus mean
training wall-clock. \texttt{+RevIN} rows wrap the same baseline in SPADE's
own RevIN module (normalization-matched, per Reviewer~4); unmarked baseline
rows use no normalization. MSE/MAE: lower is better. Once normalization is
matched and SPADE's epoch count is properly tuned (300, via a held-out
validation sweep---see text), it beats two of the four +RevIN baselines on
MSE and trails the other two by a narrow margin, but at a substantially
higher training cost than any of them.}
\label{tab:forecast}
\input{figures/tab_forecast}
\end{table}

\paragraph{Reconciling the reported error (addressing the abstract/table
inconsistency).} An earlier version reported MSE~$\approx$~0.0008 and a
``20$\times$'' improvement. We traced this to an \emph{evaluation artifact}:
computing normalization statistics on the full series (test values leaking into
the scaling) rather than on training data only. Table~\ref{tab:leak} isolates the
mechanism on a single split: global (leaky) normalization yields a markedly lower
MSE (0.015) than the correct train-only normalization (0.026)---confirming the
direction of the bias---and the original 0.0008 figure combined this leak with a
more favourable single split. We therefore \emph{retract} the 0.0008 / 20$\times$
claim; the correct, leakage-free numbers are those in Table~\ref{tab:forecast},
and all abstract and conclusion statements are updated accordingly.

\begin{table}[t]
\centering
\caption{Leak demonstration: identical SPADE model, single 80/20 split, global
(test-leaking) vs.\ train-only normalization. Leaking test statistics into the
scaler lowers the reported MSE---the artifact behind the retracted figure.}
\label{tab:leak}
\input{figures/tab_leak}
\end{table}

\subsection{Ablation and sensitivity}
\label{sec:ablation}

Table~\ref{tab:ablation} isolates SPADE's design choices on non-linear synthetic
data. The decisive factor is the \textbf{information bottleneck}: replacing the
component-wise readout with a dense residual KAN backbone (the ``coupled''
variant) collapses recovery to chance (AUROC~$0.58$), because the backbone can fit
the forecast irrespective of the adjacency. The edge-level group-lasso tells a
different, more nuanced story than we previously summarized: removing it
\emph{raises} AUROC on these small, dense synthetic graphs (0.984 without the
penalty vs.\ 0.902 with it) rather than leaving it unchanged---an 8-point gap
we should not have called ``comparable.'' This is the usual shrinkage
trade-off of an $\ell_1$-style penalty: on a graph this small and this dense,
the penalty gives up some ranking quality (AUROC) in exchange for the sparse,
thresholdable structure that matters at larger, sparser $d$ (Section~
\ref{sec:scalability}) and for the finite-sample uniqueness argument in
Proposition~\ref{prop:bottleneck}. We therefore no longer describe the
group-lasso as ranking-quality-neutral; its cost in this regime is real and
measurable, and its benefit (sparsity, and the tie-breaking role in
Proposition~\ref{prop:bottleneck}) is what we are trading for.

\paragraph{Isolating the spline edges themselves (Reviewer 2).} The ablation
above isolates the bottleneck \emph{architecture} but does not, by itself, say
whether the B-spline edge parameterization is what drives recovery, as opposed
to the bottleneck-plus-group-lasso recipe it shares with prior component-wise
neural-Granger models (Section~\ref{sec:intro}). We add a fourth variant that
keeps the architecture, optimizer, learning rate, epoch budget, seeds, and
group-lasso weight identical and replaces each B-spline edge (11 coefficients)
with a matched-capacity $1\!\to\!3\!\to\!1$ tanh MLP edge (10 coefficients).
The result: AUROC~$0.889$/F1~$0.878$ for the MLP-edge variant versus
AUROC~$0.902$/F1~$0.867$ for the B-spline variant. We report the point
estimates rather than claim a tested equivalence: with only three seeds we
do not have the power to run a meaningful significance test (unlike the
paired Wilcoxon comparisons in Table~\ref{tab:causal_stats}, which use many
more matched configuration/seed pairs), and we did not run one here rather
than report an underpowered $p$-value as if it settled the question. What
we can say is that the gap between the two variants (0.013 AUROC) is small
relative to the seed-to-seed spread we observe elsewhere in this paper
(e.g.\ Table~\ref{tab:instdag}'s $\pm0.03$--$0.15$ AUROC across five seeds),
which is suggestive, not conclusive, evidence that edge parameterization is
not the primary driver of structure recovery here; the component-wise
bottleneck is the more clearly load-bearing factor
(Table~\ref{tab:ablation}'s dense-backbone row collapses to chance, a much
larger and unambiguous effect), consistent with
Remark~\ref{rem:whatprop1claims} and the fact that
Proposition~\ref{prop:bottleneck} is architecture-agnostic.
We report this honestly rather than as a negative result to bury: SPADE's
distinctive contribution is not a structure-recovery advantage from splines
per se, but (i) the \emph{interpretability} of the recovered edge shapes,
which a scalar MLP weight cannot provide (Section~\ref{sec:splines}), and (ii)
combining the bottleneck with an explicit NOTEARS acyclicity constraint, which
GC-KAN and neural-Granger baselines do not impose. The two edge types are also
not equally expensive per training step (Table~\ref{tab:ablation}), so an
equal-epoch comparison alone does not equalize the training budget across edge
types---we report wall-clock time for exactly this reason.

\begin{table}[t]
\centering
\caption{Ablation of SPADE on non-linear synthetic data (mean over 3 seeds; no
formal significance test at this seed count). The information bottleneck is
decisive (the dense-backbone variant collapses to chance); the matched-capacity
MLP-edge variant's point estimate is close to the B-spline variant's, which we
report as suggestive rather than as a tested equivalence.
Fit time is per seed, same 150-epoch budget; ``--'' marks variants whose
wall-clock was not re-measured in this revision.}
\label{tab:ablation}
\input{figures/tab_ablation}
\end{table}

\paragraph{Sensitivity.}
\label{sec:sensitivity}
Table~\ref{tab:sensitivity} sweeps the group-lasso weight $\lambda_g$ and the
spline grid size $G$ on non-linear synthetic data. Recovery is robust
(AUROC~$0.82$--$0.98$): strong and stable for $\lambda_g\!\le\!0.01$ across grid
sizes, degrading only gracefully as the penalty is made very aggressive. We use
$\lambda_g{=}0.01$, $G{=}8$ throughout.

\begin{table}[t]
\centering
\caption{Sensitivity of SPADE to the group-lasso weight $\lambda_g$ and spline
grid size $G$ on non-linear synthetic data (mean over 3 seeds).}
\label{tab:sensitivity}
\input{figures/tab_sensitivity}
\end{table}

\subsection{Real-world causal insights: financial markets 2020--2025}
\label{sec:real}

We apply SPADE to daily closing prices of eight assets: gold (GLD), WTI crude
oil (WTI), the U.S. dollar index (DXY), the S\&P~500 (SPX), Bitcoin (BTC), silver
(SLV), copper (COPPER), and natural gas (NG), over 2020--2025 ($N{=}1{,}827$).
No ground-truth graph exists, so we treat the output as \emph{candidate}
mechanisms and validate via stability (Section~\ref{sec:stability}) rather than
F1/SHD.

\subsubsection{Learned causal graph}
Figure~\ref{fig:real_adj} shows the learned edge-importance matrix (row = effect,
column = cause), averaged over three rolling folds; Figure~\ref{fig:real_net}
renders the corresponding directed network. The seven edges scoring $\geq 0.50$
are listed in Table~\ref{tab:real_edges} with a plausibility reading. These
readings are \emph{post-hoc} interpretations for domain experts, not evidence of
verified causation; the validations below (Sections~\ref{sec:stability}--\ref{sec:validation})
test the structure beyond narrative.

\begin{figure}[t]
    \centering
    \includegraphics[width=0.78\textwidth]{figures/real_causal_adjacency.png}
    \caption{SPADE learned edge importance across eight financial assets
    (averaged over three rolling folds). Row = effect; column = cause. Tickers:
    GLD gold, WTI crude oil, DXY US dollar index, SPX S\&P~500, BTC Bitcoin, SLV
    silver, COPPER copper, NG natural gas.}
    \label{fig:real_adj}
\end{figure}

\begin{figure}[t]
    \centering
    \includegraphics[width=0.78\textwidth]{figures/real_causal_network.png}
    \caption{Directed causal network (edges with importance $\geq 0.50$; width
    $\propto$ importance). Node colour encodes asset class: precious metals
    (GLD, SLV), energy (WTI, NG, COPPER), equity (SPX), crypto (BTC), FX (DXY).}
    \label{fig:real_net}
\end{figure}

\begin{table}[t]
\centering
\caption{Significant causal edges learned by SPADE on the financial panel
(normalized importance $\geq 0.50$, averaged over three folds). Interpretations
are post-hoc plausibility readings for expert review.}
\label{tab:real_edges}
\begin{tabular}{llcp{6.8cm}}
\toprule
\textbf{Cause} & \textbf{Effect} & \textbf{Score} & \textbf{Plausibility reading} \\ \midrule
BTC    & SPX    & 0.92 & Post-2020 crypto sentiment spills into equities (risk-on/off) \\
WTI    & SPX    & 0.79 & Oil prices feed equity valuations via the inflation/energy channel \\
NG     & WTI    & 0.68 & Energy-complex co-movement across gas and oil \\
BTC    & COPPER & 0.61 & Risk-sentiment linkage to industrial-metal demand \\
DXY    & SLV    & 0.56 & Stronger USD depresses silver (inverse USD--commodity relation) \\
DXY    & WTI    & 0.51 & Oil priced in USD; a strong dollar lowers importer purchasing power \\
SPX    & COPPER & 0.50 & Equity-signalled demand expectations for industrial metals \\
\bottomrule
\end{tabular}
\end{table}

\subsubsection{Learned functional forms (spline shapes)}
\label{sec:splines}
A central interpretability claim is that edge functions expose the \emph{shape}
of a dependency. Figure~\ref{fig:splines} plots the learned B-spline for the
strongest edges: several are near-linear, while others saturate or show
threshold behaviour---information a scalar coefficient cannot convey, and which
changes the managerial reading (e.g., a saturating driver implies diminishing
returns to further exposure).

\begin{figure}[t]
    \centering
    \includegraphics[width=0.92\textwidth]{figures/real_causal_functions.png}
    \caption{Learned spline edge functions for the highest-importance edges
    (input on the normalized scale). Shapes range from near-linear to
    saturating/threshold, substantiating the interpretability of the edge
    functions.}
    \label{fig:splines}
\end{figure}

\paragraph{Lag attribution.} On synthetic data with \emph{known} lags, SPADE's
contribution-weighted lag estimate is accurate---mean absolute error 0.43 trading
days, correctly separating lag-1 from lag-3 edges. On the financial \emph{price
levels}, by contrast, the estimated lags concentrate near the middle of the
window ($\approx$3 days), reflecting the near-integrated (random-walk-like)
dynamics of prices where all recent lags are similarly predictive---consistent
with the non-stationarity concern; lag attribution is therefore most informative
on stationary or differenced series.

\subsubsection{Robustness to a stationary representation: a candid negative
result on returns}
\label{sec:returns}
Reviewer~4 asked the direct question this section has so far only motivated:
what happens if the causal analysis is redone on a genuinely stationary
representation (log-returns) instead of price levels? We reran the identical
pipeline (\texttt{scripts/extract\_real\_causal.py --returns}, same
architecture, hyperparameters, window, and three rolling folds) on daily
log-returns of the same eight assets. The honest answer is a negative result,
and we report it rather than the more flattering levels-based graph alone.
Validation MSE on returns is $0.51$/$0.79$/$0.98$ across the three folds---close
to or above the unit variance of the standardized target, i.e.\ barely better
than predicting the unconditional mean---consistent with daily returns on
liquid, actively-traded assets being close to informationally efficient and
largely unpredictable one day ahead. With essentially no predictive signal for
the group-lasso to concentrate on, the learned graph does not degenerate to
sparsity; it degenerates the \emph{other} way, to a fully-connected,
undifferentiated graph (edge density~$1.00$ in every fold, and consequently a
rolling-window Jaccard stability of exactly $1.00$---perfect but meaningless,
since a graph that is always everything is trivially identical to itself
across folds). This is not a software bug: it is what a forecasting-based
structure-learning method should honestly report when the forecasting problem
itself carries near-zero signal. Two conclusions follow, and we state both
plainly. First, this is itself evidence \emph{for} the reviewer's concern: it
shows the sparse, stable graph reported on price levels
(Sections~\ref{sec:real}--\ref{sec:stability}) most plausibly reflects shared
non-stationary trends and slower co-movement across these assets, not
validated short-horizon (one-day) causal dependence---the levels-based graph
should be read with that caveat, not as if it survived a stationarity check it
did not. Second, it delimits SPADE's applicability honestly: forecasting-based
causal discovery of the kind SPADE performs requires a data regime where
one-step-ahead structure is genuinely learnable; at daily granularity on
efficiently-priced liquid assets, that regime may not hold, and a coarser
horizon, an intraday frequency, or exogenous instruments would be needed to
say more. We did not have a positive stationary-representation result to
report, and choose to say so rather than not run the check.
\label{sec:stability}
We estimate the graph on three rolling windows and measure the mean off-diagonal
Jaccard overlap of the binarized graphs (Figure~\ref{fig:stability}), obtaining
\textbf{0.31}. To test whether this exceeds chance---the analysis Reviewers
requested---we compare against a permutation null of random graphs matched to each
fold's edge count. The observed stability is \textbf{4.7$\times$} the null mean
(0.066) with permutation $p<0.0001$: the learned structure is far more consistent
across time than random graphs of the same density, i.e.\ it reflects persistent
dependencies rather than noise.

\begin{figure}[t]
    \centering
    \includegraphics[width=0.7\textwidth]{figures/real_causal_stability.png}
    \caption{Observed cross-window Jaccard stability of the learned graph vs.\ a
    permutation null of edge-count-matched random graphs. Observed stability
    (0.31) is 4.7$\times$ the null mean ($p<0.0001$).}
    \label{fig:stability}
\end{figure}

\subsubsection{Validation beyond stability: time-reversal and regime shifts}
\label{sec:validation}
Stability shows consistency, not correctness. We add two stronger checks
requested by Reviewers.

\paragraph{Time-reversal placebo.} A genuine temporal causal method should recover
the true (forward) graph far better from forward data than from time-reversed
data; invariance to reversal would indicate spurious correlation. On non-linear
synthetic data ($d{=}10$, 3 seeds), SPADE's recovery AUROC drops from
\textbf{0.94} (forward) to \textbf{0.64} (reversed)---a 0.30 gap---confirming that
it exploits temporal precedence rather than symmetric association.

\paragraph{Ground-truth structural change (intervention).} The most direct
version of the Reviewer's request---``does SPADE recover the \emph{expected
change} in causal structure?''---requires a known change. We concatenate two SVAR
regimes with \emph{different} graphs at a known break ($t{=}1500$) and slide
SPADE across the series (Figure~\ref{fig:intervention}A). The graph-dissimilarity
signal between adjacent windows peaks around the break, and SPADE recovers the
correct regime graph on each side (AUROC~0.89 before, 1.00 after). Thus SPADE
detects the intervention \emph{and} recovers the two distinct structures---not
merely a stable graph.

\paragraph{Event-anchored regime analysis (real data, illustrative).} Applying the
same sliding-window analysis to the financial panel
(Figure~\ref{fig:intervention}B) surfaces structural shifts during the 2020 COVID
and 2021--2022 rate-hike eras; splitting at the 2022 rate-hike onset, the graph
changes in an economically coherent direction (post-2022, gold becomes a
downstream hub as flight-to-safety links appear). We stress this is
\emph{illustrative}: the rigorous, placebo-controlled version below shows real-data
single-event attribution is only marginal, so the clean intervention evidence is
the synthetic experiment.

\begin{figure}[t]
    \centering
    \includegraphics[width=\textwidth]{figures/intervention.png}
    \caption{Structural-change detection. \textbf{(A)} On two concatenated SVAR
    regimes with a known break ($t{=}1500$, dashed), the sliding-window graph
    dissimilarity spikes near the break and SPADE recovers each regime's graph
    (AUROC 0.89/1.00). \textbf{(B)} On real financial data, the detected shifts
    coincide with the 2020 COVID and 2021--2022 rate-hike eras.}
    \label{fig:intervention}
\end{figure}

\paragraph{Documented policy shock on real macro data (a candid negative result).}
To test the most direct version of the Reviewer's request, we assembled a real
macro panel from FRED (2016--2026) that includes the \emph{actual policy
instrument}---the effective Federal Funds rate---alongside 2Y/3M/10Y Treasury
yields, the broad US-dollar index, the S\&P~500, WTI, and the VIX (stationary
representation: rates/yields/VIX differenced, price indices in log-returns). The
documented shock is the March~2022 Fed rate-hike liftoff. We report the outcome
honestly, because it is instructive:
\begin{itemize}
    \item \emph{Methodological (confirmed).} On daily \emph{price levels} the
    learned structure is degenerate (the dollar index carries $<$1\% of outgoing
    influence, swamped by co-trending non-stationarity); a stationary
    representation is necessary---echoing the Reviewer's non-stationarity concern.
    \item \emph{Change magnitude (null).} A placebo-controlled, symmetric-window
    test finds the 2022 liftoff produces a structural change no larger than
    typical placebo dates (dissimilarity 0.80 vs.\ placebo mean 0.75,
    $p{=}0.43$).
    \item \emph{Direction (contrary to a naive prior).} The interest-rate block's
    causal out-influence \emph{decreases} across the shock (0.38 $\to$ 0.20)
    rather than increasing; inspection suggests the pre-2022 window is dominated
    by the 2020 COVID rate shock, when Fed actions drove markets most strongly.
\end{itemize}
We draw a deliberate, honest conclusion: on purely \emph{observational} macro
data, SPADE's graph does not cleanly recover documented interventions in a
direction simple economic priors predict, and single-event attribution is not
statistically distinguishable from chance. This is a genuine limitation of
observational causal discovery (not specific to SPADE), and it is precisely why
we anchor the intervention claim on the \emph{controlled synthetic} experiment
(Figure~\ref{fig:intervention}A), where the ground-truth change is known and SPADE
recovers it cleanly. We prefer to report this null candidly rather than
over-interpret a learned macro graph.
